# Editor / OS cruft
.DS_Store
.claude/

# let-go bytecode bundles (regenerated by build.sh)
*.lgb

# Generated AOT Go tree + compiled binaries (build.sh output).
# build/main.go, build/go.mod and build/go.sum are hand-written and tracked;
# everything else under build/ is a build artifact.
build/legmacs/
build/bin/
build/lm

# Release staging
dist/

# let-go source checkout cloned next to the repo in CI (build.sh's LETGO_DIR)
/let-go
